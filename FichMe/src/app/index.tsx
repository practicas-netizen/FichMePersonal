// Este archivo casi no hace nada — solo existe porque Expo Router
// necesita al menos un archivo de "ruta" dentro de src/app para funcionar.
// En realidad nunca lo ves en pantalla, porque _layout.tsx ya decide
// directamente qué mostrar (login, onboarding o terminal) sin pasar por aquí.
export default function Index() {
  return null;
}